# pwr_std - firmware side of the LFP power module (Power-Standard v1.0,
# maintained by the cross-project battery session; copied, not edited here).
idf_component_register(SRCS "src/pwr_std.c" INCLUDE_DIRS "include")
